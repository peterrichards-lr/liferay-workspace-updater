[alias]
# Setup the shared git hooks
setup-hooks = "config core.hooksPath .githooks"

# Run all quality checks
lint = "clippy --all-targets --all-features -- -D warnings"
fmt-check = "fmt --all -- --check"
